import SwiftUI

struct LoginView: View {
    var body: some View {
        Text("LoginView Placeholder")
            .foregroundColor(.white)
            .padding()
            .background(Color.black)
    }
}

#Preview {
    LoginView()
}
