import SwiftUI

struct PhoneLoginView: View {
    var body: some View {
        Text("PhoneLoginView Placeholder")
            .foregroundColor(.white)
            .padding()
            .background(Color.black)
    }
}

#Preview {
    PhoneLoginView()
}
