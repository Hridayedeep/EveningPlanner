import SwiftUI

struct RegisterView: View {
    var body: some View {
        Text("RegisterView Placeholder")
            .foregroundColor(.white)
            .padding()
            .background(Color.black)
    }
}

#Preview {
    RegisterView()
}
