import SwiftUI

struct AuthRootView: View {
    var body: some View {
        Text("AuthRootView Placeholder")
            .foregroundColor(.white)
            .padding()
            .background(Color.black)
    }
}

#Preview {
    AuthRootView()
}
